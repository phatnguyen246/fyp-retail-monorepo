# Account Module Docs

Tài liệu của `account` module.

## Purpose

`account` là internal module sở hữu account persistence và seed admin account.

## Docs

- [domain.md](./domain.md)

## Notes

- module này không có public HTTP API riêng
- auth behavior nằm ở `auth` module, không nằm ở đây
